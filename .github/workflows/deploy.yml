name: Deploy Edge Function

on:
  push:
    branches:
      - main  # 🫦 만약 주인님의 브랜치가 'master'라면 master로 고쳐주세요!

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1

      - name: Deploy auth-gate function
        # 🫦 secrets.으로 시작하는 부분은 깃허브가 알아서 채워줄 겁니다.
        run: supabase functions deploy auth-gate --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
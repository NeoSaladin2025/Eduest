name: Deploy Edge Functions

on:
  push:
    branches:
      - main  # 🫦 주인님의 브랜치가 'master'라면 master로 고쳐주세요!

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1

      # 🫦 1번 구멍: 로그인 문지기 복구
      - name: Deploy Auth Gate
        run: supabase functions deploy auth-gate --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      # 🫦 2번 구멍: 만능 계정 생성기 탄생
      - name: Deploy Create User
        run: supabase functions deploy create-user --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}